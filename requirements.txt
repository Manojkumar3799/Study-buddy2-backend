# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# --- Config & validation ---
pydantic==2.13.4
pydantic-settings==2.5.2
python-dotenv==1.0.1

# --- PDF processing ---
pymupdf==1.28.0

# --- Embeddings & LLM routing ---
litellm==1.57.4

# --- Vector store (pgvector via psycopg) ---
psycopg[binary]
psycopg-pool


# --- Supabase (Storage REST client) ---
supabase==2.15.2

# --- LLM (LangChain integrations) ---
langchain-core>=0.3.35
langchain-google-genai>=2.0.8
langchain-groq>=0.2.3
langchain-xai>=0.2.1

langchain-mcp-adapters>=0.1.0
langchain-mistralai

# --- MCP (Model Context Protocol) ---
mcp>=1.2.0

# --- Authentication / JWT verification ---
# PyJWT with cryptography backend supports both RS256 (JWKS/production Supabase)
# and HS256 (static secret / local Supabase emulator).
pyjwt[crypto]>=2.9.0

# --- Testing ---
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.27.0
